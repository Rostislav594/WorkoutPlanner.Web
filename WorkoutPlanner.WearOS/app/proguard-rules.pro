# Release shrinking is intentionally disabled until an optimized build is exercised
# against the real backend. Retrofit, Gson, Room and WorkManager ship their own
# consumer rules; add only evidence-based application rules when shrinking is enabled.
